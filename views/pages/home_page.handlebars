<p>Welcome to Find A Doctor! Here you can search for doctors, look at their reviews, and make appointments.</p>

{{#if docs.length}}
<h1 class="heading">Top Rated Doctors From Each Category</h1>
<ul>
    {{#each docs}}
    <li>
        <a class="speciality" href="/doctor/speciality/{{this.doctor.speciality}}">{{this.speciality}}</a>
        <dl>
            <dt>
                <a class="name" href="/doctor/{{this.doctor._id}}">{{this.doctor.name}}</a>
            </dt>
            <dd>
                {{#if this.doctor.profilePicture}}
                <img alt="image" src={{this.doctor.profilePicture}} />
                {{else}}
                <img alt="image" src="/public/images/defaultProfilePicture.jpeg" />
                {{/if}}
            </dd>

            <dt>About</dt>
            <dd>{{this.doctor.about}}</dd>
            <dt>Languages</dt>
            {{#if this.doctor.languages.length}}
            <dd>
                <ul>
                    {{#each this.doctor.languages}}
                    <li class="language">{{this}}</li>
                    {{/each}}
                </ul>
            </dd>
            {{else}}
            <dd>N/A</dd>
            {{/if}}
            <dt>Address</dt>
            <dd>{{this.doctor.address}}</dd>
            <dt>City, State, Zipcode</dt>
            <dd>{{this.doctor.city}}, {{this.doctor.state}}, {{this.doctor.zip}}</dd>
            <dt>Rating</dt>
            {{#if this.doctor.rating}}
            <dd>{{this.doctor.rating}} Stars</dd>
            {{else}}
            <dd>Doctor has not been rated yet</dd>
            {{/if}}
        </dl>
    </li>
    {{/each}}
</ul>
{{else}}
<p class="not-found">We're sorry, but there are no current Doctors.</p>
{{/if}}